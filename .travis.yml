language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "hekunyu"
  - git config user.email "hekunyu@me.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GIT_TOKEN}@${GIT_REF}" master:gh-pages
# E: Build LifeCycle

branches:
  only:
    - source
env:
 global:
   - GIT_REF: github.com/hemisu/hemisu.github.io

# .travis.yml 配置
cache:
 directories:
   - node_modules